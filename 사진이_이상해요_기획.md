# "사진이 이상해요" — 웹버전 1차 기능 기획

> PicMedic 웹버전(pywebview 기반, [README.md](README.md) 참고)의 첫 기능으로
> 무엇을 만들지 논의한 내용을 기록한 문서. 2026-09-01 대화 기준.

## 배경

웹버전에서 처음 만들 기능으로 "이 사진이 왜 안 열릴까요?"를 검토하다가,
논의 중 범위를 넓혀 **"사진이 이상해요"**로 확정함 — 완전히 못 여는 심각한
문제뿐 아니라, 열리긴 하지만 화질이 나쁜(썸네일급) 것처럼 경미한 문제도
"이상함"의 일종으로 같이 다루기로 함.

## ✅ 진입 방식: 단일 파일 (2026-09-01)

데스크톱 앱처럼 폴더 전체를 재귀 스캔하는 방식이 아니라, **파일 하나를
진단하는 흐름**으로 시작한다.

- 기술적으로는 pywebview가 일반 브라우저와 달리 샌드박스가 없어서 폴더
  재귀 스캔도 그대로 가능하지만(파이썬이 파일시스템에 직접 접근), HTML
  쪽 폴더 선택 UX가 Qt의 `QFileDialog.getExistingDirectory()`만큼
  매끈하지 않고, "이 사진 왜 이상해요?"라는 시나리오 자체가 사용자가
  파일 하나를 콕 짚어 묻는 상황이라 단일 파일 쪽이 UX적으로도 더 맞음.
- 폴더 단위 대량 진단은 이 기능이 자리 잡은 뒤 확장 방향으로 남겨둔다.

## ✅ 진단 체계 (2026-09-01)

기존 데스크톱 엔진(`core/analyzer.py`, `core/detector.py`)을 그대로
가져왔고([README.md](README.md) "core/ 동기화" 참고), 여기에 심각도 2단계
분류를 얹는다.

### 🔴 심각 — 못 열림/못 씀

| 상태 | 설명 | 비고 |
|---|---|---|
| 손상 (완전/부분) | 디코딩 실패 또는 일부만 됨 | 기존 엔진 (`FileStatus.CORRUPTED` / `PARTIAL_CORRUPTION`) |
| 이미지 아님 | 확장자만 이미지, 실제 아님 | 기존 엔진 (`NOT_AN_IMAGE`) |
| 지원 안 되는 형식 | AVIF 등 | 기존 엔진 (`UNSUPPORTED`) |
| 확장자 불일치 | 확장자 ≠ 실제 형식 (열리긴 함) | 기존 엔진 (`MISMATCH`) |
| 클라우드 미다운로드 | OneDrive/iCloud 온라인 전용, 로컬에 실체 없음 | **신규** |
| 권한/잠금 | 다른 프로그램이 파일 사용 중 / 읽기 권한 없음 | **신규** — 지금은 `_try_decode`가 모든 예외를 뭉뚱그려 "손상"으로 오분류함 |
| 안전장치 오탐 | Pillow `DecompressionBombError` 등으로 걸림 (파일 자체는 멀쩡) | **신규** — 위와 같은 이유로 지금은 "손상"으로 오분류됨 |

### 🟡 경미 — 열리지만 아쉬움

| 상태 | 설명 | 비고 |
|---|---|---|
| 저해상도 | 총 픽셀수(가로×세로) 30만 px 미만 (예: 640×480) | **신규**. 실제 폰 사진(보통 수백만~1천만 px)은 안 걸리고, 옛날 안드로이드 썸네일 캐시(보통 100×100~320×240대)는 걸러짐 |

기존 `FileStatus`(심각 쪽 판정)와는 독립적인 `is_low_resolution: bool`
플래그로 얹는다 — "정상이지만 저해상도"처럼 두 축을 조합해서 보여줄 수
있어야 하기 때문.

### ⏸ 다음으로 미룸

- **특수 색공간(CMYK 등) 뷰어 호환성 경고** — 파일 자체(Pillow 기준)는
  정상 디코딩되지만 일부 구형 뷰어(윈도우 기본 사진 앱 등)와 안 맞을 수
  있는 경우. 발생 빈도가 낮고 우선순위가 낮아 보류.
- **진짜 화질 저하(블러/노이즈) 판별** — 해상도처럼 단순 메타데이터
  비교가 아니라 이미지 분석(딥러닝급)이 필요해서 이번 범위 밖. "저해상도"
  판정만으로 1차는 충분하다고 보고, 필요성이 확인되면 다음 단계로 검토.

## 계기: 썸네일 폴더 사례

옛 스마트폰을 백업하며 딸려온 `.thumbnails` 계열 캐시 폴더의 초소형
이미지들 — 손상된 게 아니라 정상적으로 열리기 때문에 "왜 안 열려요?"
진단으로는 전혀 걸러지지 않는다는 걸 확인하면서, 범위를 "사진이
이상해요"로 넓히는 계기가 됨. (안드로이드가 갤러리 캐시용으로 만든
초소형 이미지가 사진 백업 시 같이 섞여 들어오는 경우가 흔함.)

## 구현 시 참고사항

- **권한/잠금, 안전장치 오탐 구분**: `core/analyzer.py::_try_decode`가
  지금은 모든 예외를 하나로 묶어 "손상"으로 처리 — `PermissionError`/
  `OSError`(잠금·권한)와 Pillow `Image.DecompressionBombError`(안전장치)를
  실제 디코딩 실패와 예외 타입으로 구분해야 함.
- **클라우드 미다운로드 감지**: OS별 API가 달라 크로스플랫폼 감지 방법을
  별도 조사 필요 — Windows는 파일 속성의 `FILE_ATTRIBUTE_RECALL_ON_DATA_ACCESS`,
  macOS는 iCloud 전용 API(`NSURLUbiquitousItemDownloadingStatusKey` 등).
  [CLAUDE.md](../PicMedic/CLAUDE.md)의 "OS별로 코드를 분기하지 않는다"
  원칙상, 이 부분만 최소 범위로 `sys.platform` 분기가 불가피할 가능성 높음.

## 관련 문서

- [README.md](README.md) — 웹버전 전체 방향(pywebview, core 재사용)
- [PLATFORM_EXPANSION.md](../PicMedic/PLATFORM_EXPANSION.md) — 원본 저장소의
  웹/앱 확장 검토 배경
